# Deploy

Single-VM deployment with Docker Compose (Indian cloud region):
reverse proxy (TLS), FastAPI, Flower SuperLink + ServerApp, PostgreSQL, object storage.

Copy `.env.example` to `.env` and fill in the values. **Never commit `.env`.**
